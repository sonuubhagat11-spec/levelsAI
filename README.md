--For windows arm64 and x64--
1. Download the folder
2. Unzip the folder
3. Double click "ConsoleApp2.exe"
4. Wait for the model to download and enjoy.

--For macOS and Linux arm64 andx64--
Run command: dotnet add package LLamaSharp && dotnet add package LLamaSharp.Backend.Cpu
1. Download folder
2. Unzip  the folder
3. Run these conmmands:
   - cd /path/to/levelsAI/chatbot
   This changes directory to the downloaded folder (Remember this is a placeholder path)
   - Then:
   chmod +x ConsoleApp2
   This gives permission to run the console app.
   - Finally:
     ./ConsoleApp2
     This runs the app.
4. Wait for it to download and enjoy.
   Also the source code folders are outdated and use the release folders.
